🐶🐱 Cats vs Dogs Classifier (Streamlit)

A simple Streamlit web app that classifies images as **Cat** or **Dog** using a trained Keras model.

 🚀 How to Run

1. Install dependencies:


pip install -r requirements.txt


2. Run the app:


streamlit run app.py


Deployed on Streamlit

https://dogvscats-classfication-ffxbkeeaxf5tyzs9mmwmyy.streamlit.app

 📁 Files

* **app.py** – Streamlit app
* **CatsVsDogsModel.keras** – Trained model
* **requirements.txt** – Python dependencies

📌 Note

The model file is large (>100MB).
Use **Git LFS** if pushing to GitHub.

